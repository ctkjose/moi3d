script: var scaleLength = 25.5; var distanceFromNut = 0; var distanceToNextSlot; var fretslot; var objects = moi.geometryDatabase.getSelectedObjects(); var bbox = objects.getHighAccuracyBoundingBox(); var pt = bbox.center; var factory; for ( var i = 0; i < 24; i++ ) { distanceToNextSlot = (scaleLength * 25.4 - distanceFromNut) / 17.8171537451058; distanceFromNut += distanceToNextSlot; pt.x += distanceToNextSlot; factory = moi.command.createFactory( "copy" ); factory.setInput( 0, objects ); factory.setInput( 1, bbox.center ); factory.setInput( 2, pt ); factory.commit(); }